document.addEventListener('DOMContentLoaded', function () {
  const container = document.querySelector('.resource-planner');
  if (!container) {
    return;
  }

  console.info('[ResourcePlanner] Placeholder script loaded. Interactive planning UI will be added in future iterations.');
});

